{
    // Config-only package (node, no JSX): factory functions run under jiti
    // at config load, never bundled or published. Single config, no split
    // app/node references.
    "extends": "../../tsconfig.node-base.json"
}
