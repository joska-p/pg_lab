# glaze3D-perf — Session log (append-only, one dated line per session)

- 2026-09-21 (P0): project setup — old plan archived to `codex/glaze3D/archive/PLAN-S0-S10.md`, new entry `codex/glaze3D-perf/` created (STATUS/PLAN/DECISIONS/SESSIONS/DIAG), next=P0 baseline.
- 2026-09-21 (P0): instrumentation done — dev-only `?perf=1` HUD (`lib/perf.ts`, `PerfHud.tsx`, MolCanvas render-ms + PatternCanvas pattern-ms + fps/mesh/buffer), static baseline + H1–H5 verdicts in DIAG.md, `vp check` green + `vp test` 47 passed; live timing left to user on dev server, next=P1.
- 2026-09-21 (P0): first live number (user) — cid-115374: 12 fps, render 0.66 ms, pattern 1930.48 ms, mol 523x504, pat 900x900; renderer exonerated, pattern is the bottleneck (P2 + pattern budget now highest impact).
- 2026-09-21 (P0): small live number (user) — cid-702: 3 fps, render 0.15 ms, pattern 287.69 ms, mol 438x504, pat 900x900; anomaly (fps 3 < 12 of large case) flagged for re-measure; autoRotate-off proposed as discriminating test via dev-only flag in P2.
- 2026-09-21 (P2): single RAF done — viewer loop owns it, pattern `drawPattern` via `ViewerHandle.requestPatternDraw` (throttle + eps kept), orbit Quat temporaries, `?norotate=1` flag; `vp check` green both packages, tests 60+47 passed; live P2 delta pending user re-measure.
- 2026-09-21 (P2): user live report — 4 cases (`?perf=1`, mol 476x420): 702rot 2fps/0.27/484ms, C60rot 227fps/1.42/1621ms, 702still 60/0.20/499, C60still 61/0.60/710; felt <1fps everywhere, origin smooth same machine → fps EMA declared broken (windowed throughput instead), pattern split into proj/norm/gl spans + RAF loop counter added; `vp check` green, tests 47 passed.
- 2026-09-21 (P2-fix): StrictMode dev double-mount + `loseContext()` in PatternCanvas cleanup forced the CPU fallback (pattern 500–1600 ms scaling with atom count = `drawPatternCPU` 810k px, not the GPU shader) — cleanup now deletes program+texture only, `isContextLost()` guard added; HUD shows gpu/cpu path + stale flag; CPU fallback bounded (BUF 320/256, 500 ms throttle) so it stops freezing the viewer; `vp check` green, tests 47 passed; live re-measure pending.
- 2026-09-21 (P2-fix): user re-measure confirms the fix — 702: 60 fps (loops 1), render 0.16 ms, pattern 0.98 ms gpu (0.00/0.95/0.03); C60: 58 fps (loops 1), render 0.54 ms, pattern 5.32 ms gpu (0.05/5.18/0.10); smooth even spun hard. Pattern cost is now norm-dominated (linear in atoms), GL draw negligible; no budget fix needed. Numbers in DIAG.md P2-fix delta.
- 2026-09-21 (P5): overlay layout done — single `.mol-stage` relative parent in `App.tsx` (pattern absolute background `pointer-events:none`, molecule absolute foreground, drop/drag handlers on parent, name label overlay readable above); side-by-side flex removed, no `ui` change; `vp check` green, `vp test` 47 passed; user visual OK pending.
