# **APP_NAME**

Scaffolded from `tools/app-template`. Wired to `@repo/ui` following the
contract in `codex/docs/ui-setup.md`.

```bash
vp -C apps/__APP_NAME__ dev     # dev server
vp -C apps/__APP_NAME__ build   # production build (emits stylex.css)
vp check                        # format, lint, type-check
```
