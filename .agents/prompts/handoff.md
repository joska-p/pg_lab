Summarize the current conversation into a handoff document so a fresh agent can continue the work. Save it under `_TEMP/handoff/`.

Include a "suggested skills" section naming the skills the next agent should invoke. Content already captured elsewhere (specs, plans, issues, commits, diffs) travels by path reference; the document adds only what those artifacts lack. Redact sensitive information (API keys, passwords, PII).

The user's stated focus for the next session tailors the document.
